version: 1

sitemaps:
  website:
    origin: https://www.thesentry.com
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /query-index.json
        destination: /sitemap.xml
        hreflang: en
        alternate: /{path}
